import { EditableStringDirective } from './editable-string.directive';

describe('EditableStringDirective', () => {
  it('should create an instance', () => {
    const directive = new EditableStringDirective();
    expect(directive).toBeTruthy();
  });
});
