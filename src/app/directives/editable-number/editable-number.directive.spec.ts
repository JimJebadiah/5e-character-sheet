import { EditableNumberDirective } from './editable-number.directive';

describe('EditableNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new EditableNumberDirective();
    expect(directive).toBeTruthy();
  });
});
