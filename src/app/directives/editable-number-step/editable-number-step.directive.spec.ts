import { EditableNumberStepDirective } from './editable-number-step.directive';

describe('EditableNumberStepDirective', () => {
  it('should create an instance', () => {
    const directive = new EditableNumberStepDirective();
    expect(directive).toBeTruthy();
  });
});
